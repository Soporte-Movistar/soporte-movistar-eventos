/* ============================================================
   SOPORTE MOVISTAR — js/config.js
   ⚙️ Configuración de la API centralizada del clan.
   ============================================================
   CAMBIA ESTA LÍNEA CUANDO DESPLIEGUES LA API:
   Pon la URL pública de tu API (p. ej. Render):
     window.API_BASE_URL = 'https://tu-api.onrender.com';
   En local con la API corriendo en tu PC, déjala en
     'http://localhost:3000'
   Si dejas la URL y la API no responde, el sitio degrada a
   localStorage (modo offline) y sigue funcionando.
   ============================================================ */

window.API_BASE_URL = 'http://localhost:3000';